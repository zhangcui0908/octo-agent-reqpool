# 需求判别规则（Triage Rules）

收到用户反馈时，按以下规则判断类型、分配优先级。

## 类型判断

### Bug（type:bug）
用户报告功能**行为不符合预期**：
- 明确说"报错""坏了""不工作""有问题"
- 描述实际错误信息、异常堆栈
- 某个功能操作后结果错误
- 崩溃、数据丢失、卡死

判断为 bug 的信号词：报错 / 错误 / 失败 / 异常 / bug / 崩溃 / 卡死 / 不对 / 挂了 / 不行

### Feature（type:feature）
用户**请求新能力**：
- "能不能加""建议""希望支持""需要有个功能"
- 描述想要的未来状态
- 现有产品没有的能力

判断为 feature 的信号词：建议 / 希望 / 能不能 / 需要 / 想要 / 支持 / 增强 / 优化 / 新功能

### Question（type:question）
用户**询问信息**，不是报错也不是求功能：
- 产品怎么用、什么功能存在
- 技术实现细节
- 配置怎么改

## 优先级判断

| 优先级 | 适用场景 |
|--------|---------|
| P0 | 安全漏洞、数据丢失、核心功能完全不可用、生产阻塞 |
| P1 | 重要功能异常、影响大量用户的核心体验、明确的高影响 bug |
| P2 | 次要功能问题、体验改进、一般 bug |
| P3 | 文档、美化、非功能性建议、临时方案 |

## 状态流转
```
status:inbox (新建)
   │  确认类型+优先级
   ▼
status:triaged (已分类)
   │  PM 认领
   ▼
status:prd-drafting (写PRD)
   │  PRD 完成
   ▼
status:in-review (评审中)
   │  QA/考官 review
   ├── 通过 → status:done
   └── 打回 → 回到 prd-drafting 按原因修改
```

## 转人工规则
以下情况自动@人工介入（加分项）：
- 无法明确判断类型（信息不足）
- 涉及安全/隐私敏感问题
- P0 紧急情况需要立即人工确认
- 多次打回仍无法通过评审
